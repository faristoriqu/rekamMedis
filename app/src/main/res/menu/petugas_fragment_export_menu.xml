<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        android:title="Cari BRM"
        app:showAsAction="always"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

    <item
        android:id="@+id/action_refresh"
        android:icon="@drawable/ic_refresh"
        android:title="Refresh"
        app:showAsAction="always"/>


    <item
        android:id="@+id/action_list_type"
        android:icon="@drawable/ic_view_brm"
        android:title="@string/jenis_brm"
        app:showAsAction="always">

        <menu>
            <item android:id="@+id/action_brm_aktif"
                android:title="BRM Aktif">
            </item>

            <item android:id="@+id/action_brm_all"
                android:title="Semua BRM">
            </item>
        </menu>
    </item>
</menu>